name: inutriguide-prod

x-app-env: &app-env
  env_file:
    - ${ENV_FILE:-.env}
  restart: unless-stopped
  networks:
    - public
    - private

x-backend-image: &backend-image
  image: ${DOCKER_IMAGE_NAMESPACE:-wakilrh}/inutriguide-backend:${IMAGE_TAG:-latest}

services:
  traefik:
    image: traefik:v3
    restart: unless-stopped
    env_file:
      - ${ENV_FILE:-.env}
    command:
      - --configFile=/etc/traefik/traefik.yml
    ports:
      - "80:80"
      - "443:443"
      - "7687:7687"
    volumes:
      - ./infra/traefik/traefik.prod.yml:/etc/traefik/traefik.yml:ro
      - ./infra/traefik/dynamic.prod.yml:/etc/traefik/dynamic.yml:ro
      - traefik_letsencrypt:/letsencrypt
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - public
    healthcheck:
      test: ["CMD", "traefik", "healthcheck", "--ping"]
      interval: 30s
      timeout: 10s
      retries: 5

  backend:
    <<: [*app-env, *backend-image]
    command: >
      sh -c "python manage.py migrate --noinput &&
             gunicorn config.wsgi:application --bind 0.0.0.0:8000 --workers $${GUNICORN_WORKERS:-3} --timeout $${GUNICORN_TIMEOUT:-60}"
    labels:
      - traefik.enable=true
      - traefik.docker.network=inutriguide-prod_public
      - traefik.http.routers.inutriguide-api-http.rule=Host(`${API_HOST:-api.matchcesoir.pro}`)
      - traefik.http.routers.inutriguide-api-http.entrypoints=web
      - traefik.http.routers.inutriguide-api-http.middlewares=redirect-to-https@file
      - traefik.http.routers.inutriguide-api.rule=Host(`${API_HOST:-api.matchcesoir.pro}`)
      - traefik.http.routers.inutriguide-api.entrypoints=websecure
      - traefik.http.routers.inutriguide-api.tls.certresolver=letsencrypt
      - traefik.http.routers.inutriguide-api.middlewares=secure-headers@file
      - traefik.http.services.inutriguide-api.loadbalancer.server.port=8000
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8000/api/v1/health/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      neo4j:
        condition: service_healthy

  celery_worker:
    <<: [*app-env, *backend-image]
    command: celery -A config worker -l info
    depends_on:
      backend:
        condition: service_healthy
      redis:
        condition: service_healthy

  celery_beat:
    <<: [*app-env, *backend-image]
    command: celery -A config beat -l info
    depends_on:
      backend:
        condition: service_healthy
      redis:
        condition: service_healthy

  admin_panel:
    image: ${DOCKER_IMAGE_NAMESPACE:-wakilrh}/inutriguide-admin-panel:${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks:
      - public
    labels:
      - traefik.enable=true
      - traefik.docker.network=inutriguide-prod_public
      - traefik.http.routers.inutriguide-admin-http.rule=Host(`${ADMIN_HOST:-admin.matchcesoir.pro}`)
      - traefik.http.routers.inutriguide-admin-http.entrypoints=web
      - traefik.http.routers.inutriguide-admin-http.middlewares=redirect-to-https@file
      - traefik.http.routers.inutriguide-admin.rule=Host(`${ADMIN_HOST:-admin.matchcesoir.pro}`)
      - traefik.http.routers.inutriguide-admin.entrypoints=websecure
      - traefik.http.routers.inutriguide-admin.tls.certresolver=letsencrypt
      - traefik.http.routers.inutriguide-admin.priority=1
      - traefik.http.routers.inutriguide-admin.middlewares=secure-headers@file
      - traefik.http.services.inutriguide-admin.loadbalancer.server.port=80
    depends_on:
      backend:
        condition: service_healthy

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    env_file:
      - ${ENV_FILE:-.env}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - private
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER:-inutriguide} -d $${POSTGRES_DB:-inutriguide}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    networks:
      - private
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  neo4j:
    image: neo4j:5
    restart: unless-stopped
    environment:
      NEO4J_AUTH: ${NEO4J_USER:-neo4j}/${NEO4J_PASSWORD:-change_me}
      NEO4J_server_memory_heap_initial__size: ${NEO4J_HEAP_INITIAL:-512m}
      NEO4J_server_memory_heap_max__size: ${NEO4J_HEAP_MAX:-1G}
    volumes:
      - neo4j_data:/data
      - neo4j_logs:/logs
    networks:
      - private
      - public
    labels:
      - traefik.enable=true
      - traefik.docker.network=inutriguide-prod_public
      - traefik.http.routers.inutriguide-neo4j-http.rule=Host(`${NEO4J_HOST:-neo4j.matchcesoir.pro}`)
      - traefik.http.routers.inutriguide-neo4j-http.entrypoints=web
      - traefik.http.routers.inutriguide-neo4j-http.middlewares=redirect-to-https@file
      - traefik.http.routers.inutriguide-neo4j.rule=Host(`${NEO4J_HOST:-neo4j.matchcesoir.pro}`)
      - traefik.http.routers.inutriguide-neo4j.entrypoints=websecure
      - traefik.http.routers.inutriguide-neo4j.tls.certresolver=letsencrypt
      - traefik.http.routers.inutriguide-neo4j.middlewares=ops-auth@docker,secure-headers@file
      - traefik.http.services.inutriguide-neo4j.loadbalancer.server.port=7474
      - traefik.tcp.routers.inutriguide-neo4j-bolt.rule=HostSNI(`*`)
      - traefik.tcp.routers.inutriguide-neo4j-bolt.entrypoints=bolt
      - traefik.tcp.services.inutriguide-neo4j-bolt.loadbalancer.server.port=7687
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:7474 || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 45s

  dozzle:
    image: amir20/dozzle:latest
    restart: unless-stopped
    networks:
      - public
    labels:
      - traefik.enable=true
      - traefik.docker.network=inutriguide-prod_public
      - traefik.http.middlewares.ops-auth.basicauth.users=${DOZZLE_BASIC_AUTH:?Set DOZZLE_BASIC_AUTH in .env with an htpasswd hash}
      - traefik.http.routers.inutriguide-logs-http.rule=Host(`${DOZZLE_HOST:-dozzle.matchcesoir.pro}`)
      - traefik.http.routers.inutriguide-logs-http.entrypoints=web
      - traefik.http.routers.inutriguide-logs-http.middlewares=redirect-to-https@file
      - traefik.http.routers.inutriguide-logs.rule=Host(`${DOZZLE_HOST:-logs.matchcesoir.pro}`)
      - traefik.http.routers.inutriguide-logs.entrypoints=websecure
      - traefik.http.routers.inutriguide-logs.tls.certresolver=letsencrypt
      - traefik.http.routers.inutriguide-logs.middlewares=ops-auth@docker,secure-headers@file
      - traefik.http.services.inutriguide-logs.loadbalancer.server.port=8080
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro

networks:
  public:
  private:
    internal: true

volumes:
  postgres_data:
  redis_data:
  neo4j_data:
  neo4j_logs:
  traefik_letsencrypt:
